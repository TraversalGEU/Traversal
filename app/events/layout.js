export const metadata = {
  title: "Events | Traversal",
  description: "Explore all events happening at Traversal.",
};

export default function EventsLayout({ children }) {
  return <>{children}</>;
}
