export const metadata = {
  title: "About Us | Traversal",
  description: "Learn about our mission, culture, and values.",
};

export default function AboutLayout({ children }) {
  return <>{children}</>;
}
